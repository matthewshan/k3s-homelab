apiVersion: v1
kind: Service
metadata:
  name: adk-agent
  namespace: adk-agent
spec:
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app.kubernetes.io/name: adk-agent
